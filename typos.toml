# See https://github.com/crate-ci/typos/blob/master/docs/reference.md to configure typos
[default.extend-words]
sur = "sur"
banch = "banch" # TODO: not sure if this is a typo or not
fo = "fo"
ba = "ba"
opf = "opf" # the EPUB package document format
pn = "pn" # a CSS class in test fixtures
chpater = "chpater" # a deliberate misspelling: the fail-loud filelist test
sytem = "sytem" # a deliberate misspelling: the unknown-heading prompt-file test
unparseable = "unparseable" # valid spelling; keeps reappearing, pinned for good
# Owner 250925: a typos hit is accepted here, never respelled in code or fixtures.
flate = "flate" # PDF FlateDecode filter
parms = "parms" # PDF DecodeParms key
mis = "mis" # test fixture token
nd = "nd" # test fixture token
ue = "ue" # test fixture token
ot = "ot" # test fixture token
[files]
extend-exclude = [
    "LICENSE",
    "tests/test_pdf_render.py", # base64 PDF fixtures: not words
]
